env: production
grpc_port: 9003
log_level: warn

postgres:
  max_conns: 25
  min_conns: 5
  conn_max_lifetime_minutes: 60

redis:
  pool_size: 25
  dial_timeout_ms: 300

search_cache_ttl_seconds: 60
expansion_weeks: 4
